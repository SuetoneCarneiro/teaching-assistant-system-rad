from django.http import HttpResponse


def signup(request):
    # TODO(Pedro): replace with SignUpView (CreateView + UserCreationForm, adds user to STUDENTS_GROUP)
    return HttpResponse('TODO: signup')
